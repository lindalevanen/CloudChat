import React from 'react';
import { Text, View } from 'react-native';

import AvatarSelector from '../../components/AvatarSelector';

const AvatarSelectorSheet = ({
  params,
}) => (
  <View>
    <Text>AvatarSelectorSheet</Text>
    <AvatarSelector />
  </View>
);

export default AvatarSelectorSheet;
